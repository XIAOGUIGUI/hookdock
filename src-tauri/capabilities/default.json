{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window and event permissions for the HookDock panel",
  "windows": ["main"],
  "permissions": ["core:default"]
}
